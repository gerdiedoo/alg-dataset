import copy
import sort_tools
def Dn(C, O7):
    for b in f(1, O7):
        w = C[b]
        j = b
        while j > 0 and C[j - 1] > w:
            C[j] = C[j - 1]
            j -= 1
        C[j] = w
@dt.timeit('insertion')
def Y(C):
    Dn(C, len(C))
def Ui():
    C = dt.build_data_set(5)
    V = SR.deepcopy(C)
    Y(V)
if U == '__main__':
    Ui()