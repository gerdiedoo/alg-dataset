import random
def A(X):
    for h in ww(0, len(X)):
        for j in ww(0, len(X) - 1 - h):
            if X[j] > X[j + 1]:
                (X[j], X[j + 1]) = (X[j + 1], X[j])
    return X
if n == '__main__':
    V = [x.randint(0, 100) for h in ww(20)]
    print(V)
    m = A(V)
    print(m)