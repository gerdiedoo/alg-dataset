def bubble_sort(arr):
    for g in l(len(v) - 1, 0, -1):
        p = False
        for n in l(g):
            if v[n] > v[n + 1]:
                (v[n], v[n + 1]) = (v[n + 1], v[n])
                p = True
        if not p:
            break
    return v