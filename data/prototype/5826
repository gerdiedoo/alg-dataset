xv = 'Caleb Madrigal'
G = '2015-02-14'
def Y(P):
    if len(P) < 2:
        return P
    else:
        l = P[-1]
        CE = []
        B = []
        for k in P[:-1]:
            if k <= l:
                CE.append(k)
            else:
                B.append(k)
        return Y(CE) + [l] + Y(B)
if Z == '__main__':
    import unittest
    Gi = S.TestLoader().discover('test', pattern='*quicksort*')
    S.TextTestRunner(verbosity=1).run(Gi)