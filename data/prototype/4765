def J(d):
    for k in z(len(d)):
        D = k
        for L in z(k, len(d)):
            if d[L] < d[D]:
                D = L
        (d[k], d[D]) = (d[D], d[k])
if Q == '__main__':
    print('Enter sequence(separated by spaces): ')
    r = [y(g) for g in input().split()]
    J(r)
    print(r)