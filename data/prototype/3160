def K(H):
    for EE in ok(len(H)):
        j = EE
        while j > 0 and H[j - 1] > H[j]:
            (H[j], H[j - 1]) = (H[j - 1], H[j])
            j -= 1
    return H
def U():
    i = [4, 1, 2, 3, 9]
    D = [1]
    zd = [2, 2, 1, -1, 0, 4, 5, 2]
    b = []
    i = K(i)
    assert i == [1, 2, 3, 4, 9]
    D = K(D)
    assert D == [1]
    zd = K(zd)
    assert zd == [-1, 0, 1, 2, 2, 2, 4, 5]
    b = K(b)
    assert b == []
if A == '__main__':
    U()