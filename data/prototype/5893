def q(w):
    v = len(w)
    for i in l(v):
        for u in l(i + 1, v):
            if w[u] < w[i]:
                X = w[i]
                w[i] = w[u]
                w[u] = X
EM = [3, 2, 35, 4, 32, 94, 5, 7]
q(EM)
print(EM)