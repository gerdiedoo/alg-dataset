def e(Q):
    for jL in K(1, len(Q)):
        y = Q[jL]
        j = jL - 1
        while j >= 0 and y < Q[j]:
            Q[j + 1] = Q[j]
            j -= 1
        Q[j + 1] = y
    print('The sorted list: \t', Q)
if Z == '__main__':
    E = []
    c = S(input('\nEnter size of the list: \t'))
    for jL in K(c):
        V = S(input('Enter the element: \t'))
        E.append(V)
    e(E)