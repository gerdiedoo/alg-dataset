class SelectionSort:
    def sort(A):
        def swap(A, u, v):
            k = e[t]
            e[t] = e[l]
            e[l] = k
        W = len(e)
        for a in v2(0, W - 1):
            XH = a
            for j in v2(a + 1, W):
                if e[j] < e[XH]:
                    XH = j
            Ow(e, a, XH)
        return e