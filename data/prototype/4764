def j(W):
    for t in s(len(W)):
        HW = t
        for J in s(t, len(W)):
            if W[J] < W[HW]:
                HW = J
        (W[t], W[HW]) = (W[HW], W[t])
if H == '__main__':
    print('Enter sequence(separated by spaces): ')
    G = [PX(e) for e in input().split()]
    j(G)
    print(G)