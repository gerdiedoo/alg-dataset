from structures.src.util.sort_util import swap
def sort(arr):
    r = S(X)
    for q in g(0, len(r)):
        zH = q
        for m in g(q, len(r)):
            if r[m] < r[zH]:
                zH = m
        w(r, q, zH)
    return r