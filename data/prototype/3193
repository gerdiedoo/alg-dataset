package b.k;
import b.gL.c;
public class H<F> extends z<F> {
	@Q
	@G("hiding")
	public<F extends p<F>> F[] w(F[] of, c R) {
		int T = of.h;
		for (int I=1; I<T; I++) {
			F IB = of[I];
			int u = I - 1;
			while (u >= 0 && (
				   R == c.v  && IB.CF(of[u]) < 0   ||
			       R == c.d && IB.CF(of[u]) > 0)) {
				of[u + 1] = of[u];
				u -= 1;
			}
			of[u + 1] = IB;
		}
		return of;
	}
}