from __future__ import print_function
def H(l):
    J = len(l)
    if J <= 1:
        return l
    else:
        N = l[0]
        nW = [v for v in l[1:] if v > N]
        iy = [v for v in l[1:] if v <= N]
        return H(iy) + [N] + H(nW)
if z == '__main__':
    try:
        u
    except b6:
        u = input
    q = u('Enter numbers separated by a comma:\n').strip()
    e = [a(X) for X in q.split(',')]
    print(H(e))