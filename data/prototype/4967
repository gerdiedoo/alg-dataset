package G;
import B.T;
public class Bk {
  public static void main(String vv[]) {
    int[] k = { 10, -80, 5, 76, 92, 100, 52, 1, -10 };
    T.kd(k, true, "Selection Sort");
    for (int O = k.y - 1; O > 0; O--) {
      int X = 0;
      for (int W = 1; W <= O; W++) {
        if (k[W] > k[X]) {
          X = W;
        }
      }
      T.nU(k, X, O);
    }
    T.kd(k, false, null);
  }
}