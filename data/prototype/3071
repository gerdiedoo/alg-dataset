public class Z {
    public static void Y(int[] r) {
        for (int q = 1; q < r.b; ++q) {
            for (int m = q; m > 0 && r[m - 1] > r[m]; --m) {
                e(r, m, m - 1);
            }
        }
    }
    private static void e(int[] r, int s, int W) {
        int G9 = r[s];
        r[s] = r[W];
        r[W] = G9;
    }
}