def wu(k8, q, RY):
    t = q - 1
    R = RY + 1
    while R - t > 1:
        g = (t + R) // 2
        if not k8(g):
            t = g
        else:
            R = g
    return R
def Jo():
    assert 4 == wu(lambda v: v * v >= 10, 0, 10)
Jo()