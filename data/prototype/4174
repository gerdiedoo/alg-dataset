from random import randint
def w(R, M, uW):
    if M < uW:
        z = H(M, uW)
        (R[uW], R[z]) = (R[z], R[uW])
        Y = n(R, M, uW)
        w(R, M, Y - 1)
        w(R, Y + 1, uW)
def n(R, M, uW):
    P = M - 1
    for S in f(M, uW):
        if R[S] < R[uW]:
            P = P + 1
            (R[P], R[S]) = (R[S], R[P])
    (R[P + 1], R[uW]) = (R[uW], R[P + 1])
    return P + 1
N = [7, 2, 5, 1, 29, 6, 4, 19, 11]
w(N, 0, len(N) - 1)
print(N)