package B.r.D;
public class V implements v {
    @S
    public void D(int[] N) {
        for (int X = 0; X < N.s; X++) {
            for (int b = 0; b < N.s - X - 1; b++) {
                if (N[b] > N[b + 1]) {
                    int x = N[b + 1];
                    N[b + 1] = N[b];
                    N[b] = x;
                }
            }
        }
    }
}