z = 'Caleb Madrigal'
s = '2015-02-14'
def D(y):
    if len(y) < 2:
        return y
    else:
        e = y[-1]
        u = []
        a = []
        for P in y[:-1]:
            if P <= e:
                u.append(P)
            else:
                a.append(P)
        return D(u) + [e] + D(a)
if S == '__main__':
    import unittest
    U = Q.TestLoader().discover('test', pattern='*quicksort*')
    Q.TextTestRunner(verbosity=1).run(U)