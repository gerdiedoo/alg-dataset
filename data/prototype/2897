package Kb.G.f;
public class l implements x {
    @L
    public void f(int[] i) {
        for (int H = 0; H < i.FT; H++) {
            for (int nx = 0; nx < i.FT - H - 1; nx++) {
                if (i[nx] > i[nx + 1]) {
                    int r = i[nx + 1];
                    i[nx + 1] = i[nx];
                    i[nx] = r;
                }
            }
        }
    }
}