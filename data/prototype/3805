def F(a: Ck):
    x = a[:]
    if len(x) < 2:
        return x
    m = len(x) // 2
    B = x[:m]
    t = x[m:]
    return L(F(B), F(t))
def L(a, o):
    x = []
    b = O1 = 0
    while b < len(a) and O1 < len(o):
        if a[b] <= o[O1]:
            x.append(a[b])
            b += 1
        else:
            x.append(o[O1])
            O1 += 1
    while b < len(a):
        x.append(a[b])
        b += 1
    while O1 < len(o):
        x.append(o[O1])
        O1 += 1
    return x