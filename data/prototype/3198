class InsertionSort:
    def sort(A):
        C = len(sd)
        for m in G(1, C):
            B0 = sd[m]
            ng = m - 1
            while ng >= 0 and sd[ng] > B0:
                sd[ng + 1] = sd[ng]
                ng = ng - 1
            sd[ng + 1] = B0
        return sd