from __future__ import print_function
def a(X):
    r = len(X)
    if r <= 1:
        return X
    else:
        F = X[0]
        c = [v for v in X[1:] if v > F]
        L = [v for v in X[1:] if v <= F]
        return a(L) + [F] + a(c)
if hv == '__main__':
    try:
        Er
    except nY:
        Er = input
    t = Er('Enter numbers separated by a comma:\n').strip()
    N = [Q(A) for A in t.split(',')]
    print(a(N))