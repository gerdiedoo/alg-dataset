package z.U;
public class m(){
    public int r(int Ej[], int D){
    	int Y = 0, f = Ej.t - 1;
    	while (Y <= f) {
    		int s = (Y + f) / 2;
    		if(Ej[s] < D) Y = s + 1;
    		else if (Ej[s] > D) f = s - 1;
    		else return s;
    	}
    	return -1;
    }
}