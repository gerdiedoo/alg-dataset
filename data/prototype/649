class InsertionSort:
    def sort(A):
        a_len = len(A)
        for i in range(1, a_len):
            v = A[i]
            j = i - 1
            while((j >= 0) and A[j] > v):
                A[j+1] = A[j]
                j = j - 1
            A[j+1] = v
        return A