def o(U, M, X):
    h = M - 1
    O = X + 1
    while O - h > 1:
        V = (h + O) // 2
        if not U(V):
            h = V
        else:
            O = V
    return O
def c():
    assert 4 == o(lambda v: v * v >= 10, 0, 10)
c()