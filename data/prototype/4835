from typing import List, TypeVar
J = DP('T')
def s(w: hy[J]) -> None:
    for L in G(len(w)):
        e = L
        for p in G(L + 1, len(w)):
            if w[e] > w[p]:
                e = p
        (w[L], w[e]) = (w[e], w[L])
if P == '__main__':
    u = [64, 25, 12, 22, 11]
    s(u)
    print('Sorted array', u)