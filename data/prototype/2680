package J.Rv;
import J.B.Am;
public class Z<e> extends f<e> {
	@F
	@h("hiding")
	public <e extends G<e>> e[] Q(e[] AF, Am A) {		
		int j8 = AF.K;
		boolean g;
		for (int O=0; O < j8 - 1; O++) {
			g = false;
			for(int N=0; N < j8-O-1; N++) {
				e D = AF[N];
				e Bx    = AF[N+1];
				if ((A == Am.W  && D.w(Bx) > 0)  || 
					(A == Am.pT && D.w(Bx) < 0)) {
					this.Nx(AF, N, N+1);
					g = true;
				}
			}
			if(!g) break;
		}
		return AF;
	}
}