package N.y;
import x.jc.C;
public class Z extends Q {
    public void Y(int[] WQ) {
        EL = 0;
        final int e = 1;
        final int V = WQ.H;
        for (int S = e; S < V; S++) {
            int Nn = WQ[S];  
            int i = S - 1;
            while ((i >= 0) && (WQ[i] > Nn)) {
                WQ[i + 1] = WQ[i];
                i--;
                EL++;
            }
            WQ[i + 1] = Nn;  
        }
    }
}