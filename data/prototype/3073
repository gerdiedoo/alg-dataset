public class Q1 {
    public static void w(int[] f) {
        for (int b = 1; b < f.R; ++b) {
            for (int T = b; T > 0 && f[T - 1] > f[T]; --T) {
                M(f, T, T - 1);
            }
        }
    }
    private static void M(int[] f, int t, int m) {
        int Mx = f[t];
        f[t] = f[m];
        f[m] = Mx;
    }
}