def S(q: y):
    Y = q[:]
    if len(Y) < 2:
        return Y
    H = len(Y) // 2
    t = Y[:H]
    lo = Y[H:]
    return F(S(t), S(lo))
def F(q, G):
    Y = []
    a = u = 0
    while a < len(q) and u < len(G):
        if q[a] <= G[u]:
            Y.append(q[a])
            a += 1
        else:
            Y.append(G[u])
            u += 1
    while a < len(q):
        Y.append(q[a])
        a += 1
    while u < len(G):
        Y.append(G[u])
        u += 1
    return Y