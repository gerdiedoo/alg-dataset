import random
def iO(f):
    for b in G(0, len(f)):
        for j in G(0, len(f) - 1 - b):
            if f[j] > f[j + 1]:
                (f[j], f[j + 1]) = (f[j + 1], f[j])
    return f
if W == '__main__':
    g = [N.randint(0, 100) for b in G(20)]
    print(g)
    i = iO(g)
    print(i)