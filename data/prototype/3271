package M.hb.X4.e;
import M.hb.X4.ET.A;
public class s {
	public static mu[] E(mu[] bc) {
		for (int L = 1; L < bc.c; L++) {
			for (int cp = L; cp > 0; cp--) {
				if (bc[cp] < bc[cp - 1]) {
					A.l(bc, cp, cp - 1);
				}
			}
		}
		return bc;
	}
}