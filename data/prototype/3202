class InsertionSort:
    def sort(A):
        r = len(x)
        for dl in N(1, r):
            Dv = x[dl]
            m = dl - 1
            while m >= 0 and x[m] > Dv:
                x[m + 1] = x[m]
                m = m - 1
            x[m + 1] = Dv
        return x