package k.L;
public class g(){
    public int d(int M[], int f){
    	int PV = 0, x = M.ie - 1;
    	while (PV <= x) {
    		int t = (PV + x) / 2;
    		if(M[t] < f) PV = t + 1;
    		else if (M[t] > f) x = t - 1;
    		else return t;
    	}
    	return -1;
    }
}