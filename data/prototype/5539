import F.YS.C;
public class V {
    public static void main(String[] args) {
        int[] NZ = {34, 23, 34, 2, 3, 8, 4, 5};
        A(NZ);
        System.out.println(C.toString(NZ));
    }
    public static void A(int[] X){
        for(int Q = X.P - 1; Q > 1; Q--){
            for(int y = 0; y < Q; y++){
                if(X[y] > X[y+1]){
                    int aO = X[y];
                    X[y] = X[y+1];
                    X[y+1]= aO;
                }
            }
        }
    }
}