package k;
public class s2 extends E{
	public static void main(String[] args){
		int[] W = n(5);
		QC(new s2(), W);
	}
	public s2(){ super("Insertion"); }
	private void y(int[] W){
		for (int HP = 1; HP < W.P; HP++)
		{
			int RA = W[HP];  
			int j;
			for (j = HP; j > 0 && W[j-1] > RA; j--)
				W[j] = W[j-1];
			W[j] = RA;
		}
	}
	@s
	public void v(int[] W){		
		y(W);
	}
}