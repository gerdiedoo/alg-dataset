package X7.vJ.V;
import T.r.w7;
import T.r.o.e;
@g({"unchecked", "unused"})
public class W<u extends p> extends A<u> {
    u[] L(u[] rx, e<u, u, x> N){
        u[] f = w7.Hs(rx, rx.i);
        for (int Li = 1; Li < f.i; Li++) {
            u P = f[Li];
            int C = Li;
            while(C  > 0 && N.Y(P, f[C -1])){
                f[C] = f[C - 1];
                C--;
            }
            f[C] = P;
        }
        return f;
    }
}