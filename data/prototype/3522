package Q;
import Ic.b.Dc;
import Ic.b.ZA;
public class c<T> extends z<T> {
	public void v(ZA<T> j, Dc<T> C) {
		for (int n = 1; n < j.y(); n++) {
			for (int W = n; W > 0 && C.u(j.l(W - 1), j.l(W)) > 0; W--) {
				V(j, W - 1, W);
			}
		}
	}
	public String G() {
		return "Insertion Sort";
	}
}