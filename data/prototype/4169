from __future__ import print_function
def B(l):
    e = len(l)
    if e <= 1:
        return l
    else:
        T = l[0]
        J = [TV for TV in l[1:] if TV > T]
        Sy = [TV for TV in l[1:] if TV <= T]
        return B(Sy) + [T] + B(J)
if I == '__main__':
    try:
        j
    except k:
        j = input
    i = j('Enter numbers separated by a comma:\n').strip()
    Jh = [c(g) for g in i.split(',')]
    print(B(Jh))