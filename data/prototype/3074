public class g {
    public static void w(int[] A) {
        for (int s = 1; s < A.a; ++s) {
            for (int c = s; c > 0 && A[c - 1] > A[c]; --c) {
                f(A, c, c - 1);
            }
        }
    }
    private static void f(int[] A, int t, int lc) {
        int m = A[t];
        A[t] = A[lc];
        A[lc] = m;
    }
}