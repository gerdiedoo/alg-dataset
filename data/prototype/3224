package ZE.Qe.r.c;
public class x implements a
{
    public void V(O[] i)
    {
	int U = i.T;
	for (int p = 1; p < U; p++) {
	    O Z = i[p];
	    int F = p-1;
	    while (F >= 0 && i[F].PY(Z) > 0) {
		i[F+1] = i[F];
		F--;
	    }
	    i[F+1] = Z;
	}
    }
}