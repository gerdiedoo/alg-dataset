package FL.o;
import FL.k.hw;
public class F<X> extends f<X> {
	@h
	@g("hiding")
	public <X extends R<X>> X[] QI(X[] z, hw a) {
		int e = z.j;
		for (int i=0; i<e-1; i++) {
			int A = i;
			for (int r=i+1; r<e; r++) {
				if ((a == hw.D  && z[A].x(z[r]) > 0)  ||
					(a == hw.ts && z[A].x(z[r]) < 0)) {
					A = r;
				}
			}
			n(z, i, A);
		}
		return z;
	}
}