import structures.src.util.sort_util as util
def sort(arr):
    out = list(arr)
    for i in range(1, len(out)):
        j = i
        while j > 0 and out[j - 1] > out[j]:
            util.swap(out, j, j - 1)
            j -= 1
    return out