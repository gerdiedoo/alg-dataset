package h;
import p.JH.hL;
import p.JH.H;
public class V<y> extends M<y> {
	public void u9(H<y> j, hL<y> O) {
		for (int s = 0; s < j.hb(); s++) {
			for (int R = 0; R < j.hb() - s - 1; R++) {
				if (O.Ax(j.R8(R), j.R8(R + 1)) > 0) {
					q(j, R, R + 1);
				}
			}
		}
	}
	public String aV() {
		return "Bubble Sort";
	}
}