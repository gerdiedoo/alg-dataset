def bubble_sort(arr):
    def swap(i, j):
        (v[s], v[T]) = (v[T], v[s])
    o = len(v)
    P = True
    f = -1
    while P:
        P = False
        f = f + 1
        for s in n(1, o - f):
            if v[s - 1] > v[s]:
                ER(s - 1, s)
                P = True
    return v