package F.vW.x;
public class k implements A {
    @i
    public void x(int[] G) {
        for (int s = 1; s < G.Ja; s++) {
            for (int g = s; g > 0 && G[g - 1] > G[g]; g--) {
                int d = G[g];
                G[g] = G[g - 1];
                G[g - 1] = d;
            }
        }
    }
}