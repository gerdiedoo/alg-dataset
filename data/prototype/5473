package S.i;
public class P(){
    public int N(int W[], int V){
    	int nJ = 0, l = W.t - 1;
    	while (nJ <= l) {
    		int Q = (nJ + l) / 2;
    		if(W[Q] < V) nJ = Q + 1;
    		else if (W[Q] > V) l = Q - 1;
    		else return Q;
    	}
    	return -1;
    }
}