package u.zT.e.a;
public class M implements O
{
    public void X(R[] o)
    {
	int Ii = o.E;
	for (int y = 1; y < Ii; y++) {
	    R D = o[y];
	    int Q = y-1;
	    while (Q >= 0 && o[Q].A(D) > 0) {
		o[Q+1] = o[Q];
		Q--;
	    }
	    o[Q+1] = D;
	}
    }
}