def NZ(B, AJ):
    return True if B < AJ else False
def Y(A):
    if len(A) > 1:
        K = len(A) // 2
        d = Y(A[:K])
        E8 = Y(A[K:])
        C = []
        while d != [] and E8 != []:
            C.append(d.pop(0) if NZ(d[0], E8[0]) else E8.pop(0))
        C += d + E8
        return C
    else:
        return A