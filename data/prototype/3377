package t.H.m.S;
public class B {
	public static void w(U[] X){
		for(int rB = 1; rB < X.ef; rB++){
			int BQ = X[rB];
			int y = rB - 1;
			while( (y >= 0) && (X[y] > BQ) ){
				X[y+1] = X[y];
				y--;
			}
			X[y+1] = BQ;
		}
	}
}