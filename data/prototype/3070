public class I {
    public static void SJ(int[] f) {
        for (int M = 1; M < f.h; ++M) {
            for (int u = M; u > 0 && f[u - 1] > f[u]; --u) {
                q(f, u, u - 1);
            }
        }
    }
    private static void q(int[] f, int O, int s) {
        int rl = f[O];
        f[O] = f[s];
        f[s] = rl;
    }
}