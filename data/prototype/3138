def C(v, fA):
    for H in g(1, fA):
        yw = v[H]
        S = H
        while S > 0 and v[S - 1] > yw:
            v[S] = v[S - 1]
            S = S - 1
        v[S] = yw
if P == '__main__':
    print('Enter the elements in the array')
    v = dK(F1(Hh, input().split(' ')))
    fA = len(v)
    C(v, fA)
    print('Sorted Array: ', end='')
    for H in g(0, fA):
        print(v[H], end=' ')