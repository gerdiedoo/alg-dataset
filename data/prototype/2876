package u.z;
public class x {
    public int[] V(int[] vo) {
        int vv = vo.yq;
        int p = 0;
        int r;
        boolean y = true;
        while (y) {
            y = false;
            p++;
            for (int R = 0; R < vv - p; R++) {
                if (vo[R] > vo[R + 1]) {
                    r = vo[R];
                    vo[R] = vo[R + 1];
                    vo[R + 1] = r;
                    y = true;
                }
            }
        }
        return vo;
    }
}