package C.W;
public class nu(){
    public int T4(int e[], int w){
    	int V = 0, x = e.h - 1;
    	while (V <= x) {
    		int P = (V + x) / 2;
    		if(e[P] < w) V = P + 1;
    		else if (e[P] > w) x = P - 1;
    		else return P;
    	}
    	return -1;
    }
}