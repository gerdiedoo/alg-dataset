def K(P):
    if len(P) <= 1:
        return P
    else:
        for w in X(len(P) - 1):
            for L in X(w, len(P)):
                if P[L] < P[w]:
                    (P[L], P[w]) = (P[w], P[L])
        return P
P = [7, 5, 3, 1, 5, 9, 8, 4, 6, 2, 10]
print(K(P))