def to(K, M):
    q = 0
    HO = len(K) - 1
    while q < HO:
        W = q + (HO - q) // 2
        if K[W] < M:
            q = W + 1
        else:
            HO = W
    if K[q] == M:
        return q
    return 'not found'
X = j(vS(0, 30, 2))
t = to(X, 13)
print(t)