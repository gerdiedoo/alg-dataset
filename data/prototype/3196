class InsertionSort:
    def sort(A):
        VN = len(Z)
        for L in H(1, VN):
            w = Z[L]
            a = L - 1
            while a >= 0 and Z[a] > w:
                Z[a + 1] = Z[a]
                a = a - 1
            Z[a + 1] = w
        return Z