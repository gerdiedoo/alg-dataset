package PX.y;
import PX.U.C8;
public class Ht<eP> extends II<eP> {
	@a_
	@O("hiding")
	public<eP extends w<eP>> eP[] L3(eP[] nU, C8 p) {
		int st = nU.q;
		for (int Re=1; Re<st; Re++) {
			eP S = nU[Re];
			int m = Re - 1;
			while (m >= 0 && (
				   p == C8.nA  && S.t(nU[m]) < 0   ||
			       p == C8.dB && S.t(nU[m]) > 0)) {
				nU[m + 1] = nU[m];
				m -= 1;
			}
			nU[m + 1] = S;
		}
		return nU;
	}
}