package D.VA.o.G;
public class S implements f {
	@V8
	public void G4(int[] X) {
		for (int P = 1; P < X.t; P++) {
			int y = X[P];
			int u = P - 1;
			while (u >= 0 && X[u] > y) {
				X[u + 1] = X[u];
				u--;
			}
			X[u + 1] = y;
		}
	}
}