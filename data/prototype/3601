def merge_in_place(a, b):
    try:
        i = Dy.index(None) - 1
    except d:
        return Dy
    V = len(W) - 1
    Y = len(Dy) - 1
    while V >= 0:
        if i >= 0 and Dy[i] > W[V]:
            Dy[Y] = Dy[i]
            i -= 1
        else:
            Dy[Y] = W[V]
            V -= 1
        Y -= 1