class BubbleSort:
    def sort(A):
        def swap(A, u, v):
            lC = e[O]
            e[O] = e[r]
            e[r] = lC
            return
        q = len(e)
        for dp in OS(0, q - 1):
            for a in OS(0, q - 1 - dp):
                if e[a + 1] < e[a]:
                    AU(e, a, a + 1)
        return e