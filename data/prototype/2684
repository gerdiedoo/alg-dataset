import numpy as np
def z(g):
    def swap(F, P):
        (g[F], g[P]) = (g[P], g[F])
    P2 = len(g)
    yP = True
    A = -1
    while yP:
        yP = False
        A = A + 1
        for As in Q(1, P2 - A):
            if g[As - 1] > g[As]:
                G(As - 1, As)
                yP = True
    for As in Q(0, P2):
        print(g[As])
if u3 == '__main__':
    g = p.array([5, 6, 1, 23, 87, 78, 98, 76])
    z(g)