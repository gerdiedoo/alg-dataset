package hb.k.w;
public class kO implements cc {
    @Q
    public void w(int[] o) {
        for (int b = 0; b < o.t - 1; b++) {
            int JV = b;
            for (int y = b + 1; y < o.t; y++) {
                if (o[JV] > o[y]) {
                    JV = y;
                }
            }
            int l = o[JV];
            o[JV] = o[b];
            o[b] = l;
        }
    }
}