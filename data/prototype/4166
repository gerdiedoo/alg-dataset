def D(j):
    if len(j) <= 1:
        return j
    else:
        x = j.pop()
    A = []
    Q = []
    for X in j:
        if X < x:
            A.append(X)
        else:
            Q.append(X)
    return D(A) + [x] + D(Q)
j = [7, 5, 3, 1, 5, 9, 8, 4, 6, 2, 10]
print(D(j))