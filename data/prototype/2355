import u.nf.*;
class D {
    public int f(int[] X, int ng) {
        for (int Q = 0; Q < X.E; Q++) {
            if (X[Q] == ng || X[Q] > ng) {
                return X[Q];
            }
        }
        return -1;
    }
}