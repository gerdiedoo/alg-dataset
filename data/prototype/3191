package X.Z;
import X.h;
public class f extends h {
    @w
    public void SK(C[] D) {
        for (int QY = 0; QY < D.B; QY++) {
            for (int t = QY; t > 0; t--) {
                if (z(D[t], D[t - 1]))
                    Z2(D, t, t - 1);
            }
        }
    }
    public static void X(C[] D) {
        new f().SK(D);
    }
}