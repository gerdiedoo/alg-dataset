package q.g;
public class f implements A {
    public void AG(int H, int u[]) {
        int c, p, d;
        for (p = 0; p < H - 1; p++) {
            for (c = 0; c < H - p - 1; c++) {
                if (u[c] > u[c + 1]) {
                    d = u[c];
                    u[c] = u[c + 1];
                    u[c + 1] = d;
                }
            }
        }
    }
}