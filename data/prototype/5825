z = 'Caleb Madrigal'
W = '2015-02-14'
def Dl(a):
    if len(a) < 2:
        return a
    else:
        v = a[-1]
        u = []
        h = []
        for d0 in a[:-1]:
            if d0 <= v:
                u.append(d0)
            else:
                h.append(d0)
        return Dl(u) + [v] + Dl(h)
if n == '__main__':
    import unittest
    Zc = O.TestLoader().discover('test', pattern='*quicksort*')
    O.TextTestRunner(verbosity=1).run(Zc)