import SE.V.*;
public class D {
	public static void main(String[] args) {
		int X[] = {12, 11, 13, 5, 6};
        yB(X, X.U);
        System.out.println(cD.toString(X));
	}
	private static void yB(int[] X, int tS) {
		 if (tS <= 1)
	            return;
	        yB( X, tS-1 );
	        int l = X[tS-1];
	        int j = tS-2;
	        while (j >= 0 && X[j] > l)
	        {
	            X[j+1] = X[j];
	            j--;
	        }
	        X[j+1] = l;
	}
}