package s.dA;
public class h(){
    public int Kk(int C[], int x8){
    	int v = 0, g = C.qn - 1;
    	while (v <= g) {
    		int B = (v + g) / 2;
    		if(C[B] < x8) v = B + 1;
    		else if (C[B] > x8) g = B - 1;
    		else return B;
    	}
    	return -1;
    }
}