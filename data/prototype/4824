package H;
public class a<u extends P<u>> implements qZ<u> {
    @fd
    public void A(u[] n) {
        for (int s = 0; s < n.y; s++) {
            int Q = hP(n, s, n.y - 1);
            xk.f(n, s, Q);
        }
    }
    private int hP(u[] n, int T, int S) {
        u hP = n[T];
        int N = T;
        for (int Q = T; Q <= S; Q++) {
            if (n[Q].Z(hP) < 0) {
                hP = n[Q];
                N = Q;
            }
        }
        return N;
    }
}