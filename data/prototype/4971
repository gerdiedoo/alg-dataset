package K;
import dB.j;
public class M {
  public static void main(String y5[]) {
    int[] S8 = { 10, -80, 5, 76, 92, 100, 52, 1, -10 };
    j.Hm(S8, true, "Selection Sort");
    for (int Q = S8.h - 1; Q > 0; Q--) {
      int v = 0;
      for (int z = 1; z <= Q; z++) {
        if (S8[z] > S8[v]) {
          v = z;
        }
      }
      j.X(S8, v, Q);
    }
    j.Hm(S8, false, null);
  }
}