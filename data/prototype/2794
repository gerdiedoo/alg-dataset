package DY;
public class N<t extends S<t>> implements O<t> {
    @G
    public void w(t[] U) {
        boolean r = false;
        for (int C = 0; !r && C < U.m; C++) {
            r = true;
            for (int xf = 0; xf < U.m; xf++) {
                if (U[C].e(U[xf]) < 0) {
                    T.l(U, C, xf);
                    r = false;
                }
            }
        }
    }
}