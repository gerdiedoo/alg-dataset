package k.u;
import H.C.o;
public class G extends cF {
    public void w(int[] g) {
        f = 0;
        final int Ow = 1;
        final int N = g.R;
        for (int s = Ow; s < N; s++) {
            int J = g[s];  
            int tF = s - 1;
            while ((tF >= 0) && (g[tF] > J)) {
                g[tF + 1] = g[tF];
                tF--;
                f++;
            }
            g[tF + 1] = J;  
        }
    }
}