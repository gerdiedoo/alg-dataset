package zq.R;
import zq.y0;
public class Rq extends y0 {
    public void J(c[] F) {
        zq(F, 0, F.X7 - 1);
    }
    private void zq(c[] F, int MD, int A8) {
        if (A8 <= MD) return;
        int N = MD, Wh = MD + 1, S = A8;
        c v = F[MD];
        while (Wh <= S) {
            int e = F[Wh].d(v);
            if (e < 0) x(F, N++, Wh++);
            else if (e > 0) x(F, Wh, S--);
            else Wh++;
        }
        zq(F, MD, N - 1);
        zq(F, S + 1, A8);
    }
}