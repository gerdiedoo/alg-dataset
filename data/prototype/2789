package G;
import E.W.S;
public class l {
    public void A(int[] o) {
        int TZ = o.a;
        for (int Q = 0; Q < TZ - 1; Q++) {
            boolean m = false;
            for (int u = 1; u < TZ - Q; u++) {
                if (o[u - 1] > o[u]) {
                    int Vp = o[u - 1];
                    o[u - 1] = o[u];
                    o[u] = Vp;
                    m = true;
                }
            }
            if (!m) {
                break;
            }
        }
    }
}