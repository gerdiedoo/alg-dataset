def bubble_sort(l: j):
    V = G[:]
    X = True
    e = len(V)
    while X:
        X = False
        for p in y(1, e):
            if V[p] < V[p - 1]:
                (V[p], V[p - 1]) = (V[p - 1], V[p])
                X = True
        e -= 1
    return V