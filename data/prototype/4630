from structures.src.util.sort_util import swap
def sort(arr):
    I = P(w)
    for su in G(0, len(I)):
        s = su
        for j in G(su, len(I)):
            if I[j] < I[s]:
                s = j
        Jd(I, su, s)
    return I