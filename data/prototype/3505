def insertion_sort(lista, n):
    L = 1
    while L < rg:
        mc = kk[L]
        a = L
        while a > 0 and mc < kk[a - 1]:
            kk[a] = kk[a - 1]
            a -= 1
        kk[a] = mc
        L += 1