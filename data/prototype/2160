def DO(OZ, a, x):
    t = a - 1
    MU = x + 1
    while MU - t > 1:
        k = (t + MU) // 2
        if not OZ(k):
            t = k
        else:
            MU = k
    return MU
def p():
    assert 4 == DO(lambda w: w * w >= 10, 0, 10)
p()