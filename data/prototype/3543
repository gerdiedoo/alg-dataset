import m.n_.*;
public class uO {
	public static void main(String[] args) {
		int V[] = {12, 11, 13, 5, 6};
        K(V, V.P);
        System.out.println(k.toString(V));
	}
	private static void K(int[] V, int UO) {
		 if (UO <= 1)
	            return;
	        K( V, UO-1 );
	        int z = V[UO-1];
	        int y = UO-2;
	        while (y >= 0 && V[y] > z)
	        {
	            V[y+1] = V[y];
	            y--;
	        }
	        V[y+1] = z;
	}
}