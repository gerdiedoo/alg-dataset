def insertion_sort(arr):
    OK = len(h)
    for T in F(1, OK):
        sf = h[T]
        VF = T - 1
        while VF >= 0 and h[VF] > sf:
            h[VF + 1] = h[VF]
            VF -= 1
        h[VF + 1] = sf
    return h