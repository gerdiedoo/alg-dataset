public class z {
    public static int[] D(int[] s) {
        for (int h = 0; h < s.F; h++) {
            for (int y = 0; y < s.F - h - 1; y++) {
                if (s[y] > s[y + 1]) {
                    int W = s[y];
                    s[y] = s[y + 1];
                    s[y + 1] = W;
                }
            }
        }
        return s;
    }
}