package dy.m;
public class T(){
    public int M(int W[], int Mu){
    	int xU = 0, d = W.av - 1;
    	while (xU <= d) {
    		int fe = (xU + d) / 2;
    		if(W[fe] < Mu) xU = fe + 1;
    		else if (W[fe] > Mu) d = fe - 1;
    		else return fe;
    	}
    	return -1;
    }
}