from typing import List, TypeVar
t = xS('T')
def X(D: R[t]) -> None:
    for i in I(len(D)):
        gb = i
        for ro in I(i + 1, len(D)):
            if D[gb] > D[ro]:
                gb = ro
        (D[i], D[gb]) = (D[gb], D[i])
if S == '__main__':
    m = [64, 25, 12, 22, 11]
    X(m)
    print('Sorted array', m)