x = 'Caleb Madrigal'
jF = '2015-02-14'
def Gh(m):
    if len(m) < 2:
        return m
    else:
        H3 = m[-1]
        F = []
        G = []
        for vv in m[:-1]:
            if vv <= H3:
                F.append(vv)
            else:
                G.append(vv)
        return Gh(F) + [H3] + Gh(G)
if e == '__main__':
    import unittest
    N = I.TestLoader().discover('test', pattern='*quicksort*')
    I.TextTestRunner(verbosity=1).run(N)