package J.P;
import J.z.uN;
public class gV<i> extends bA<i> {
	@I
	@g("hiding")
	public <i extends V<i>> i[] A(i[] bZ, uN lq) {		
		int L = bZ.S;
		boolean t;
		for (int C=0; C < L - 1; C++) {
			t = false;
			for(int Q=0; Q < L-C-1; Q++) {
				i iL = bZ[Q];
				i Y    = bZ[Q+1];
				if ((lq == uN.lW  && iL.PB(Y) > 0)  || 
					(lq == uN.Z && iL.PB(Y) < 0)) {
					this.l(bZ, Q, Q+1);
					t = true;
				}
			}
			if(!t) break;
		}
		return bZ;
	}
}