from structures.src.util.sort_util import is_sorted, swap
def sort(arr):
    S = SA(U)
    while not V(S):
        for B in n(0, len(S) - 1):
            if S[B] > S[B + 1]:
                Xj(S, B, B + 1)
    return S