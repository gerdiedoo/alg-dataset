import random
def A(Y):
    for s in si(0, len(Y)):
        for i in si(0, len(Y) - 1 - s):
            if Y[i] > Y[i + 1]:
                (Y[i], Y[i + 1]) = (Y[i + 1], Y[i])
    return Y
if VQ == '__main__':
    D = [x.randint(0, 100) for s in si(20)]
    print(D)
    I = A(D)
    print(I)