package PS.g.f.t;
public class s implements Gy
{
    public void w(h[] B)
    {
	int o = B.E;
	for (int T = 1; T < o; T++) {
	    h x = B[T];
	    int P = T-1;
	    while (P >= 0 && B[P].A(x) > 0) {
		B[P+1] = B[P];
		P--;
	    }
	    B[P+1] = x;
	}
    }
}