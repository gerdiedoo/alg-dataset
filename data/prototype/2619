def bubble_sort(arr):
    def swap(i, j):
        (wc[R], wc[y]) = (wc[y], wc[R])
    h = len(wc)
    Xm = True
    w = -1
    while Xm:
        Xm = False
        w = w + 1
        for R in K(1, h - w):
            if wc[R - 1] > wc[R]:
                f(R - 1, R)
                Xm = True
    return wc