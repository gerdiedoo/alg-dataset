package tK.U;
import tK.z.jV;
public class E<Q> extends dr<Q> {
	@S
	@A("hiding")
	public<Q extends p<Q>> Q[] x8(Q[] d, jV eQ) {
		int J4 = d.O;
		for (int I=1; I<J4; I++) {
			Q w = d[I];
			int L = I - 1;
			while (L >= 0 && (
				   eQ == jV.v  && w.x(d[L]) < 0   ||
			       eQ == jV.Pm && w.x(d[L]) > 0)) {
				d[L + 1] = d[L];
				L -= 1;
			}
			d[L + 1] = w;
		}
		return d;
	}
}