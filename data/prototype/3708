def merge_sort(LIST):
    K = []
    Y = []
    while len(O) > 1:
        U = V(O)
        d = max(O)
        K.append(U)
        Y.append(d)
        O.remove(U)
        O.remove(d)
    if O:
        K.append(O[0])
    Y.reverse()
    return K + Y