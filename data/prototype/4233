def g(H):
    if len(H) <= 1:
        return H
    R = []
    U = []
    e = H[0]
    for r in H[1:]:
        if r <= e:
            R.append(r)
        else:
            U.append(r)
    return g(R) + [e] + g(U)
D = [8, 3, 19, 43, 17, 22, 1, 4, 15, 2, 16, 32, 5]
F = g(D)
print(F)