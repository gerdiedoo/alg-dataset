package ic;
import wF.Q.tt;
import wF.Q.c;
public class H<VP> extends W<VP> {
	public void d(c<VP> j, tt<VP> B) {
		for (int n = 1; n < j.R(); n++) {
			for (int xl = n; xl > 0 && B.u(j.S(xl - 1), j.S(xl)) > 0; xl--) {
				Z(j, xl - 1, xl);
			}
		}
	}
	public String I() {
		return "Insertion Sort";
	}
}