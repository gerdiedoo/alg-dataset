import java.util.*;
class Solution {
    public int solve(int[] rooms, int target) {
        for (int i = 0; i < rooms.length; i++) {
            if (rooms[i] == target || rooms[i] > target) {
                return rooms[i];
            }
        }
        return -1;
    }
}