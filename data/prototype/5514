import random
def UD(Y):
    for Hl in x(0, len(Y)):
        for t in x(0, len(Y) - 1 - Hl):
            if Y[t] > Y[t + 1]:
                (Y[t], Y[t + 1]) = (Y[t + 1], Y[t])
    return Y
if TB == '__main__':
    D = [l.randint(0, 100) for Hl in x(20)]
    print(D)
    i = UD(D)
    print(i)