package Yc.X.P.Zm;
public class nw {
	public static void kG(q[] F6, int J, int O) {
		int u = 0;
		if (J < O) {
			u = s(F6, J, O);
			kG(F6, J, u);
			kG(F6, u + 1, O);
		}
	}
	private static int s(q[] qw, int J, int O) {
		int Qn = qw[J];
		int n = J - 1;
		int t = O;
		int V = 0;
		while (true) {
			while (qw[n] < Qn) {
				n++;
			}
			while (qw[t] > Qn) {
				t--;
			}
			if (n >= t) {
				return t;
			}
			V = qw[n];
			qw[n] = qw[t];
			qw[t] = V;
		}
	}
}