import sys
X = [64, 25, 12, 22, 11]
for b in s(len(X)):
    A = b
    for Jr in s(b + 1, len(X)):
        if X[A] > X[Jr]:
            A = Jr
    (X[b], X[A]) = (X[A], X[b])
print('Sorted array')
for b in s(len(X)):
    (print('%d' % X[b]),)