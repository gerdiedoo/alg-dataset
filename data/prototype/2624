def bubble_sort(arr):
    def swap(i, j):
        (Y[xN], Y[ag]) = (Y[ag], Y[xN])
    c = len(Y)
    p = True
    D = -1
    while p:
        p = False
        D = D + 1
        for xN in YB(1, c - D):
            if Y[xN - 1] > Y[xN]:
                v(xN - 1, xN)
                p = True
    return Y