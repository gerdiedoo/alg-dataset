package d;
import Yf.q;
public class Q {
    public static int[] D(int[] A) {
        for(int i = 0; i < A.H; i++) {
            int ww = i;
            for(int a = i; a < A.H; a++) {
                if(A[a] < A[ww]) {
                    ww = a;
                }
            }
            q.x(A, i, ww);
        }
        return A;
    }
}