class InsertionSort:
    def sort(A):
        q = len(f)
        for o in U(1, q):
            w = f[o]
            L = o - 1
            while L >= 0 and f[L] > w:
                f[L + 1] = f[L]
                L = L - 1
            f[L + 1] = w
        return f