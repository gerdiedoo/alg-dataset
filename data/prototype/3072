public class vs {
    public static void L_(int[] g) {
        for (int i = 1; i < g.t; ++i) {
            for (int T = i; T > 0 && g[T - 1] > g[T]; --T) {
                e(g, T, T - 1);
            }
        }
    }
    private static void e(int[] g, int O, int wY) {
        int E = g[O];
        g[O] = g[wY];
        g[wY] = E;
    }
}