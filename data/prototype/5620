def Mk(X):
    y = len(X)
    for i in PN(1, y):
        u = X[i]
        j = i
        while j > 0 and X[j - 1] > u:
            X[j] = X[j - 1]
            j -= 1
        X[j] = u
def Dc():
    X = [a(HH) for HH in input().split()]
    Mk(X)
    print(X)
if C == '__main__':
    Dc()